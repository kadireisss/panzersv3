# PANZER V3 — .gitignore
node_modules/
.env
.env.local
.env.production
.vercel/
.next/
*.log
.DS_Store
Thumbs.db

# PHP monorepo aynası (npm run sync-php-monorepo). Vercel’de tam PHP için: sync → bu satırı sil → php-app’i commit’le.
php-app/

# Supabase agent-skills (yerel: npx skills add supabase/agent-skills)
.agents/
.claude/
.qoder/
skills-lock.json
